<h1>Customers</h1>
<% if @customers != [] %>
  <table class="table table-hover">
    <tbody>
      <% @customers.each do |c| %>
      <tr>
        <td><%= link_to c.name, company_path(c) %></td>
        <td>Customer Revenue(in thousands): $<%= c.revenue  %></td>
      </tr>
      <% end %>
    </tbody>
  </table>
  <% else %>
    <h3>You have don't have customers yet.
      Better sell some software or you'll be fired soon.</h3>
  <%end %>
